// Wire types live in @skyspecs/contracts. Re-export here so the rest of the api keeps its import path.
export type {
  User,
  Collection,
  CollectionMovie,
  CollectionWithMovies,
  CollectionStats,
  AnnotationPatch,
  MovieSearchResult,
  MovieSearchPage,
} from "@skyspecs/contracts";
